I built a 3d rendering engine from scratch that renders to the command line.
Features:
  - Realtime (>60fps) performance using JIT compilation via Numba in no-python mode.
  - Supports loading 3d models from .obj files complete with UV texture mapping.
  - Supports full color output in compatible terminal emulators.
  - 3D color texture mapping.
